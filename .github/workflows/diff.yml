on: push
name: CI
jobs:
  detect-diff:
    name: detect-diff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: add file
        run: yarn create-file
      - name: git add -N
        run: git add -N .
      - name: git diff
        run: git diff --exit-code
